import styled from 'styled-components';
import { Collapse } from 'antd';

export const StyledCommonCollapse = styled(Collapse)`
  .javascript {
    padding: 20px;
  }

  .ant-collapse-content-box {
    padding: 0;
  }

  strong {
    font-size: 1.5rem;
    color: #efbd5a;
  }
`;
